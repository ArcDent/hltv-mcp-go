services:
  hltv-mcp:
    build: .
    ports:
      - "8082:8082"
    environment:
      - HTTP_PORT=8082
      - HTTP_HOST=0.0.0.0
      - HLTV_CHROME_PATH=/headless-shell/headless-shell
    restart: unless-stopped
